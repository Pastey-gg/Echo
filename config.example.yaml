general:
  host: "0.0.0.0"
  port: 8080
  use_gzip: true

database:
  dsn: "postgres://user:password@localhost:5432/echo?sslmode=disable"

pastes:
  max_files: 5
  max_file_size: 350000
  id_length: 8
  token_length: 32